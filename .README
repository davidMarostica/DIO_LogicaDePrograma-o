# Classificador de Nível de Herói 🎮

Este projeto faz parte do desafio de Lógica de Programação da DIO. Ele implementa duas funcionalidades principais:

1. Classificação de nível baseado em XP (Pontos de Experiência)
2. Classificação de nível baseado em Vitórias/Derrotas

## 🚀 Funcionalidades

### 1. Classificação por XP
Calcula o nível do herói com base em seus pontos de experiência (XP):

| XP | Nível |
|----|--------|
| ≤ 1.000 | Ferro |
| 1.001 a 2.000 | Bronze |
| 2.001 a 5.000 | Prata |
| 5.001 a 6.000 | Ouro |
| 6.001 a 7.000 | Platina |
| 7.001 a 8.000 | Ascendente |
| 8.001 a 10.000 | Imortal |
| ≥ 10.001 | Radiante |

### 2. Classificação por Vitórias
Calcula o nível do herói baseado no número de vitórias:

| Vitórias | Nível |
|----------|--------|
| < 10 | Ferro |
| 11 a 20 | Bronze |
| 21 a 50 | Prata |
| 51 a 80 | Ouro |
| 81 a 90 | Diamante |
| 91 a 100 | Lendário |
| ≥ 101 | Imortal |

## 🛠️ Tecnologias Utilizadas

- JavaScript
- Node.js
- readline-sync (para entrada de dados)

## ⚙️ Como Executar

1. Clone o repositório:
```bash
git clone https://github.com/davidMarostica/DIO_LogicaDePrograma-o.git
```

2. Entre no diretório do projeto:
```bash
cd DIO_LogicaDePrograma-o
```

3. Instale as dependências:
```bash
npm install
```

4. Execute o programa:
```bash
node Herois/heroi.js
```

## 📋 Exemplos de Uso

### Classificação por XP
```javascript
let heroi = "David";
let xp = 8500;
let nivel = calculaNivelHeroi(xp);
// Resultado: O Herói David tem 8500 pontos de XP e está no nível: Imortal
```

### Classificação por Vitórias/Derrotas
```javascript
console.log(calcularNivel(85, 20));
// Resultado: O Herói tem saldo de 65 está no nível de Diamante
```

## 👨‍💻 Autor 

David Aparecido Da Silva
## 📄 Licença

Este projeto está sob a licença MIT.
